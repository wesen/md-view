# Tasks

## TODO

- [x] Add tasks here

- [x] Fix image path resolution: serve the markdown file's directory as static assets or rewrite <img src> to absolute paths so relative images render correctly
- [x] Add copy-to-clipboard button/icon on all <pre><code> blocks with visual feedback
- [x] Verify and fix mermaid diagram rendering: ensure embedded mermaid.js + init script works correctly with theme toggle and live reload
